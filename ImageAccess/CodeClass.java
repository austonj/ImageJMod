/*** IP-LAB* ImageProcessing Laboratories** Biomedical Image Group (BIG)* Swiss Federal Institute of Teachnology Lausanne (EPFL)**/import ij.*;public class CodeClass {	/**	* Apply a binary threshold to an image.	*/	public static ImageAccess threshold(ImageAccess in, double threshold) {		int nx = in.getWidth();		int ny = in.getHeight();		ImageAccess out = new ImageAccess(nx, ny);		IJ.write("Thresholded output using a threshold of "+threshold+"");		// Add your code here				int i; int j;		for (i=0; i < nx; i++) {			for (j=0; j < ny; j++) {				//				double GreyLevel = in.getPixel(i,j);				if (j==0) {					IJ.write("(j=0 and i="+i+") has grey Level= "+GreyLevel+"");				}												if (GreyLevel < threshold) {				// threshold the image					out.putPixel(i,j,0.0);				}				else {					out.putPixel(i,j,128.0);				}			}		}		//putArrayPixels(MyProcessedImage);				return out;	}	/**	* Change background using threshold to an image.	*/	public static ImageAccess fusionBackground(ImageAccess in, ImageAccess bckg, 							double thresholdLow, double thresholdHigh) {		int nx = in.getWidth();		int ny = in.getHeight();		ImageAccess out = new ImageAccess(nx, ny);		IJ.write("Question3");		// Add your code here						return out;	}	/**	* Add a white frame to an image.	*/	public static ImageAccess addGrayFrame(ImageAccess in, int width) {		int nx = in.getWidth();		int ny = in.getHeight();		// Add your code here						ImageAccess out = new ImageAccess(nx, ny);		IJ.write("Question4");		return out;	}}